[package]
name = "{{name}}"
version = "0.1.0"
edition = "2026"

[[targets]]
name = "{{name}}"
kind = "bin"
path = "src/main.syl"

[capabilities]
profile = "none"
network = []
environment = []
max_memory_bytes = 65536
